# Copy this file to .env (gitignored) and adjust if needed.
# docker-compose.yml auto-loads .env from the repo root for ${VAR} substitution.
# These are local-only dev defaults, safe to keep as-is for `docker compose up`.

POSTGRES_USER=alerts
POSTGRES_PASSWORD=alerts
POSTGRES_DB=price_alerts
