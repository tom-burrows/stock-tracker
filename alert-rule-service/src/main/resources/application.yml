spring:
  application:
    name: alert-rule-service
  datasource:
    url: jdbc:postgresql://localhost:5432/${POSTGRES_DB:price_alerts}
    username: ${POSTGRES_USER:alerts}
    password: ${POSTGRES_PASSWORD:alerts}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health
